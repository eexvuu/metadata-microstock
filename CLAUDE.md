# CLAUDE.md

Guidance for this repo lives in one file so every agent reads the same thing,
whichever tool it runs in. Read it before writing code:

@AGENTS.md

Repo-specific skills are in `.claude/skills/` — `add-resource`, `change-schema`
and `ship`. Use them for those tasks rather than reconstructing the steps.
