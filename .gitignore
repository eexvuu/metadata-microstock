node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.env.local
.nitro
.tanstack
# Left over from the Cloudflare era; master still uses it.
.wrangler
.output
.vinxi
__unconfig*
todos.json

# The SQLite file and the copies deploy.sh takes before migrating.
data/
*.db
*.db-journal
*.db-wal
*.bak

# Scratch media for manual testing — and gemini-key.txt lives in there.
sample/
.release-package.json
